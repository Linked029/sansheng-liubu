# 三省六部 V3.0 业务与功能重构方案

日期：2026-08-01
版本：V3.0（规划稿）
范围：个人 RAG 学习工具 · 本机调度 + Web 阅读界面

---

## 1. 三版现状盘点

### 1.1 原版 Android（LYiHub/labs-ArchiveAssistant）

- Kotlin + Jetpack Compose，包名 `com.lyihub.archiveassistant`。
- 六部为固定分类体系，主题不可增删改。
- 数据存 DataStore Preferences；远程 AI 四引擎 + 本地 LiteRT LM。
- 支持网页抓取（Jsoup）、文档内容提取（PDFBox）、URI 导入、拖拽/剪贴板导入。
- 有完整奏折阅读/审阅界面（含奏折概览、封面序列、折叠屏适配）。
- 原 README 自述：**AI 三省六部推荐尚未实现**，六部固定不可管理。

### 1.2 V1.0 Web（backup-local-v1）

- React + Vite + TypeScript + Zustand + Dexie.js 的 Web 移植版。
- 放开自定义主题；IndexedDB 持久化；JSON 导出/导入 + 10 份自动快照。
- 拖拽导入 PDF/Markdown/TXT/图片；12 条六部案例数据。
- 简化版 Canvas 奏折阅读器；本地三款古风字体（约 19 MB）。
- `OPTIMIZATION_PLAN.md` 规划了 P0-P3 共 18 项优化。

### 1.3 V2.0 Web（archive-assistant-web @ v2.0.0）

- 实现了大部分优化项：代码分割、Vercel 配置、ErrorBoundary、PWA、深色模式、快捷键、minisearch、DOCX 解析、Markdown 渲染、lucide 图标、AI 预置模板、Markdown 导出、增强版奏折阅读器。
- 新增弹窗内拖拽导入（图片/PDF/DOCX/TXT/MD）。
- 移除案例 seed、独立 FileDropZone、本地字体文件、优化方案文档。

### 1.4 重构理由

1. 三版都停留在“归档工具”：只能手动/半自动把内容放进六部，缺少**采集、筛选、审批、学习、复习**的完整业务闭环。
2. “每天固定时间 AI 抓取信息源”无法靠纯浏览器页面可靠完成。
3. 微信公众号在浏览器端无法直接抓取，需要 RSS 桥接或本机调度。
4. “优质文章”是主观标准，需要每部独立配置，不能全局一统。
5. RAG 学习不止是读文章，还需要批注、对语料提问、按遗忘曲线复习。

---

## 2. 产品定位

**三省六部 V3.0 = 个人 RAG 学习工具。**

- 只面向个人，不做多租户 SaaS。
- 本地优先：数据存本机 SQLite，Web 应用是阅读/审批界面。
- 六部 = 六个内容领域分工频道，每部独立配置信息源、偏好卡和抓取时间。
- 三省 = 文章生命周期的流程分工：中书省采集拟折、门下省审批、尚书省归档执行。
- 核心闭环：**采集 → 拟折 → 待批 → 归档 → 阅读 → 批注 → 复习 → 问答**。

---

## 3. 业务模型：三省六部

### 3.1 三省职责

| 省 | 职责 | 产品形态 |
|----|------|----------|
| 中书省 | 采集 + 拟折 | AI 按每部时间抓取信息源，生成候选奏折（标题/简介/全文/归类理由） |
| 门下省 | 审核 | 用户批阅候选奏折：准奏 / 驳回 / 打回重拟；可设置 AI 自动准奏阈值 |
| 尚书省 | 归档 + 执行 | 准奏后归入六部，进入阅读、复习、问答语料 |

### 3.2 六部分工（默认语义）

| 部 | 领域 | 信息源示例 |
|----|------|-----------|
| 吏 · 名籍 | 人物、作者、组织、人脉 | 技术博客作者、公众号作者、人物访谈 |
| 户 · 府库 | 财务、账单、资产、订阅 | 微信公众号、财务博客、账单分析 |
| 礼 · 典章 | 学习、技术、知识体系 | 技术周刊、论文、深度教程 |
| 兵 · 行令 | 健康、效率、工具、战术 | 健康管理、效率工具、开源项目 |
| 刑 · 稽核 | 法规、稽核、合规、防坑 | 法规标准、消费者警示、开源协议 |
| 工 · 营造 | 工程、项目、实操 | 工程博客、项目文档、部署教程 |

六部默认固定为这六个频道，但保留“自定义主题”能力用于扩展。

### 3.3 奏折状态机

```
候选（中书省） → 待批（门下省） → 已归档（尚书省） → 已读 → 复习中（SM-2） → 已掌握
```

- 源抓取失败或拟折异常生成“异常奏折”，标注原因，不静默丢失。
- 被驳回的文章记入驳回日志；同一源连续低质可提示更换源。
- 已掌握后停止自动排期，仍可在知识库中手动翻出。

---

## 4. 信息源与偏好卡

### 4.1 信息源形态

每个部可挂任意数量的信息源，三种形态统一转为“文章条目”：

| 形态 | 说明 |
|------|------|
| Feed 源 | RSS/Atom；微信公众号走 RSS 桥（RSSHub 本地实例 / wechat2rss 等） |
| URL 源 | 无 Feed 的网页/列表页，AI 定时抓取并提取正文 |
| 手动源 | 粘贴正文、微信收藏导出、浏览器剪藏兜底 |

### 4.2 Source 字段

```text
id, ministryId, name, kind(feed|url|manual), location,
enabled, lastFetchedAt, lastStatus, failStreak
```

### 4.3 每部偏好卡

每部独立配置，六部不统一：

```text
ministryId
description        // 偏好描述，如“关注个人财务，不要荐股和营销软文”
excludeKeywords[]  // 排除关键词
excludeDomains[]   // 排除域名
dailyLimit         // 每日拟折上限，默认 3，可调 1-10
scheduleCron       // 本部抓取时间，默认 08:00，可单独覆盖
```

### 4.4 去重规则

- URL 完全匹配去重。
- 标题相似度去重（与已有语料比较）。
- 近 7 天被驳回的内容进入“弃用清单”，不再重复拟折。

---

## 5. 每日调度流程

1. 到点后，调度器按部读取启用中的信息源。
2. 抓取 Feed / URL / 手动导入内容。
3. 提取正文，做去重。
4. 按该部偏好卡，AI 打分排序，生成标题、简介、全文摘要、归类理由。
5. 取 `dailyLimit` 上限篇目，写入候选奏折。
6. Web 应用打开时“门下省”显示待批红点；应用在线时浏览器 Notification 提醒。
7. 源全部失败时生成异常奏折，写明失败原因。

---

## 6. 功能清单

### 6.1 从 V2.0 保留

- 六部归档库与条目增删改
- 全文搜索（minisearch 保留，后续可升级 FTS5）
- 内容类型过滤（网页/图像/文档）
- 奏折阅读器（Canvas + Markdown 双模式、字体大小、盖印）
- JSON 导出/导入、Markdown 导出（Obsidian）
- 深色模式、全局快捷键、ErrorBoundary、PWA 外壳
- AI 引擎配置与预置模板（DeepSeek/Grok/Kimi/Qwen/GLM/Ollama）

### 6.2 调整

- 首页改为“三省工作台”：今日待批、今日到期复习、今日采集统计 + 六部宫格。
- 删除首页“待办奏折”最近条目区（由复习队列替代）。
- 移除内置案例 seed。
- 六部默认固定为分工频道；自定义主题作为扩展能力保留。

### 6.3 新增

- 门下省：候选奏折批阅（准奏 / 驳回 / 打回重拟 / 自动准奏阈值）
- 中书省：采集日志（抓取记录、失败原因、驳回记录）
- 信息源管理：每部源的增删改、启停、抓取测试
- 偏好卡管理：每部偏好、排除规则、每日上限、抓取时间
- 本机调度器：Node/TS 服务，Windows 计划任务/开机自启
- 阅读批注（批红）：阅读与复习时保存个人想法
- SM-2 间隔重复：忘记/困难/良好/轻松四档，`1→3→7→15→30` 天递进
- 每日复习队列：今日到期优先，每日 3-10 条
- RAG 问答：本地索引检索 + 用户配置的 AI 引擎生成答案
- 学习统计：每日复习完成率、每部到期数量、周学习量
- 桌面通知：应用在线时通知“待批 / 复习到期”

### 6.4 不做（本轮）

- 多租户 / 账号体系 / 多人协作
- 云端自动同步（后期可加 WebDAV/坚果云）
- 邮件或微信消息推送
- 完整课程体系、积分、社区等重产品功能

---

## 7. 数据模型

```text
Ministry(部)      id, title, order, icon, color
Source(信息源)     id, ministryId, name, kind, location, enabled,
                   lastFetchedAt, lastStatus, failStreak
Preference(偏好卡) ministryId, description, excludeKeywords, excludeDomains,
                   dailyLimit, scheduleCron
Item(奏折)         id, ministryId, sourceId?, contentType, title, summary,
                   fullText, sourceUrl, documentFormat, fileName,
                   status(candidate|pending|archived|read|reviewing|mastered),
                   qualityScore, aiReason, createdAt, approvedAt, archivedAt,
                   readAt
Review(复习)       itemId, stage, dueAt, intervalDays, ease, reviewCount,
                   lastReviewedAt, status(reviewing|mastered)
Annotation(批注)   id, itemId, createdAt, text
FetchLog(采集日志) id, date, ministryId, sourceId, status, itemCount, error
RejectLog(驳回)    id, itemId, sourceId, reason, createdAt
```

存储：SQLite（`better-sqlite3`），调度器写入，Web 端通过本地 HTTP API 读取。

---

## 8. 技术架构

### 8.1 本机调度器

- Node/TS 常驻服务或计划任务。
- `rss-parser` / `undici` 抓取 Feed 与网页。
- readability 类库提取正文。
- 复用现有 AI transport 逻辑生成候选奏折。
- Windows 计划任务或开机自启；错过时间后下次启动补抓当天。

### 8.2 Web 前端

- 基于 `archive-assistant-web` V2.0 代码改造。
- 存储层从 IndexedDB 切换为调用本地 API（Hono/Express + better-sqlite3）。
- 保留现有 React 页面与奏折阅读器，新增门下省、复习、问答页面。

### 8.3 RAG 问答

- 已归档全文在本地建索引（FTS5 全文 + 可选 sqlite-vec 向量）。
- 提问时本地检索 Top 5-10 片段。
- 默认把“问题 + 片段”发给用户配置的 AI 引擎生成答案。
- 支持 Ollama 本地模型实现全离线。

### 8.4 微信公众号

- 浏览器端不直接抓微信。
- 优先自建 RSSHub 实例或 wechat2rss 作为 Feed 源接入。
- 手动导出/剪藏作为兜底。

### 8.5 数据迁移

- 提供 V2.0 IndexedDB JSON 导出 → SQLite 导入工具。
- `topics` → `Ministry`，`items` → `Item(status=archived)`。

---

## 9. 实施阶段

### P0：采集审批闭环（最小可用）

- SQLite 数据模型 + 本地 API
- 本机调度器：按部时间抓取、去重、AI 拟折
- 信息源管理与偏好卡管理
- 门下省待批（准奏/驳回/打回重拟）
- 数据迁移工具

### P1：阅读与复习

- 奏折阅读批注（批红）
- 已读状态
- SM-2 复习队列与每日回顾
- 学习统计

### P2：RAG 问答

- 本地索引与检索
- 问答页面
- Ollama 离线模式

### P3：打磨

- 桌面通知完善
- 采集日志 UI
- PWA 离线体验
- 导出增强（含复习记录）

---

## 10. 验收标准

- 每部按配置时间完成抓取；失败源生成异常奏折。
- 门下省可准奏/驳回；准奏后进入对应部归档。
- 奏折可阅读、批注；读完标记已读。
- 复习队列按 SM-2 到期出现；四档反馈正确推进间隔。
- 问答能引用本地语料片段并给出带来源的回答。
- 全部数据可从 SQLite 导出 JSON/Markdown 备份。

---

## 11. 风险与对策

| 风险 | 对策 |
|------|------|
| 微信反爬 / RSS 桥不稳定 | 源失败进异常奏折；支持更换桥接服务或手动导入 |
| 本机不常开错过抓取 | 开机自启 + 下次启动补抓当天；时间可逐部覆盖 |
| AI 输出格式漂移 | 沿用 JSON 容错解析；OpenAI 系加 `response_format` |
| 浏览器定时不可靠 | 已由本机调度器解决，Web 只负责展示与审批 |
| 数据量增长 | SQLite FTS5 索引 + 全文截断 + 定期清理弃用清单 |
